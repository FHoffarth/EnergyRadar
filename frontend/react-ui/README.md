# EnergyRadar React UI

The QtWebEngine desktop application loads this Vite production build from the
PyInstaller bundle. Live data and settings are supplied through QWebChannel;
the browser build does not invent device or measurement data.

## Run Locally

**Prerequisites:**  Node.js


1. Install the locked dependencies:
   `npm ci`
2. Run the app:
   `npm run dev`
